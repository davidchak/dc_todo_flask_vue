
# FLASK-VUE loacl configs
# Whether or not to use the minified scripts.
VUE_USE_MINIFIED	True

# If True, scripts will be served from the local instance.
# VUE_SERVE_LOCAL	False

# Passes a subdomain parameter to the generated Blueprint. Useful when serving assets locally from a different subdomain.
# VUE_LOCAL_SUBDOMAIN	None

# False	If a CDN resource url starts with //, prepend ‘https:’ to it.
# VUE_CDN_FORCE_SSL

# flask_vue.config	Individualized setting about each script. It supports to config CDN scripts which do not have local instance.
# VUE_CONFIGURATION
